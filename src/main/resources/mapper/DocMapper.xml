<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.xufengtian14.badwiki.mapper.IDocMapper">

    <resultMap id="docMap" type="com.github.xufengtian14.badwiki.model.Doc">
        <id property="id" column="id"></id>
        <result property="spaceId" column="space_id"></result>
        <result property="name" column="name"></result>
        <result property="size" column="size"></result>
        <result property="parentId" column="parent_id"></result>
        <result property="hasChild" column="has_child"></result>
        <result property="editor" column="editor"></result>
        <result property="editTime" column="edit_time"></result>
        <result property="deleted" column="deleted"></result>
        <result property="creator" column="creator"></result>
        <result property="createTime" column="create_time"></result>
        <result property="modifier" column="modifier"></result>
        <result property="modifyTime" column="modify_time"></result>
    </resultMap>

    <select id="seleteDocById" resultMap="docMap">
        SELECT * from doc
        where id = #{id};
    </select>

    <update id="updateDoc">
        update doc
        set
        <if test="space_id != null">
            space_id = #{spaceId},
        </if>
        <if test="name != null and name != ''">
            name = #{name},
        </if>
        <if test="size != null">
            size = #{size},
        </if>
        <if test="parentId != null">
            parent_id = #{parentId},
        </if>
        <if test="hasChild != null">
            has_child = #{hasChild},
        </if>
        <if test="editor != null">
            editor = #{editor},
        </if>
        <if test="editTime != null">
            edit_time = #{editTime},
        </if>
        <if test="deleted != null">
            deleted = #{deleted},
        </if>
        <if test="creator != null">
            creator = #{creator},
        </if>
        <if test="createTime != null">
            create_time = #{createTime},
        </if>
        <if test="modifier != null">
            modifier = #{modifier},
        </if>
        <if test="modifyTime != null">
            modify_time = #{modifyTime},
        </if>
        id = #{id}
        where id = #{id}
    </update>

    <insert id="insertDoc" useGeneratedKeys="true" keyProperty="id">
        insert doc(space_id, name, size, parent_id, has_child, editor, edit_time, deleted, creator, create_time, modifier, modify_time )
        values ( #{spaceId}, #{name}, #{size}, #{parentId}, #{hasChild}, #{editor}, #{editTime}, #{deleted}, #{creator}, #{createTime}, #{modifier}, #{modifyTime} )
    </insert>

    <update id="deleteDocById">
        update doc set delete = true where id = #{id}
    </update>

</mapper>